# Electron build resources

Drop `icon.ico` (256x256, multi-res Windows ICO) and an optional
`installerIcon.ico` here, then uncomment the `icon:` line in
`../electron-builder.yml`.

`background.png` (NSIS sidebar) is optional.
